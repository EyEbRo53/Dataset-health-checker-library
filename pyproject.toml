[project]
name = "dataset-health-checker"
version = "0.1.0"
description = "Zero-config dataset health checks for ML datasets"
dependencies = ["Pillow", "numpy"]

[project.scripts]
dataset-health-check = "dataset_health.cli:main"
